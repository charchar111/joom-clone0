doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body 
        header 
            h1 NOOM
        main 
            #welcome   
                form(action="")
                    input(placeholder="room name" required ) 
                    button Enter Room
            div#myStream(hidden)
                video(width="400" height="400" autoplay playsinline )#face-video
                div#controller
                    button#mute Mute
                    button#camera Camera off
                    select#cameras

        script(src="/socket.io/socket.io.js") 
        script(src="/static/js/app.js") 
        