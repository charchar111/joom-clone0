doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body 
        header 
            h1 NOOM
        main 
            ul.message-logs
            form(action="").nickname-send
                input(type="text" placeholder="write nickname" required)
                button  Save

            form(action="").message-send
                input(type="text" placeholder="write msg" required)
                button  Send 
        script(src="/static/js/app.js") 
        